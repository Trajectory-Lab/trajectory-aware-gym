# Drift Note

This published set is archived instead of being kept under the formal
`orz57k-tool` results.

- `config_snapshot.yaml` uses `val_size=50`, while the canonical Orz57K tool
  protocol requires `val_size=300`.
- Although `gepa_budget.mode` is `medium`, the published run is not healthy:
  seed `123` in `20260422T192956Z` recorded `eval.failed=39` and
  `eval.timed_out=39`.
- The run is kept for provenance only; it should not be treated as a formal
  Orz57K result.

Published timestamp retained here:

- `20260422T192956Z`
