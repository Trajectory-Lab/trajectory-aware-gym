# Drift Note

This published set is archived instead of being kept under the formal
`orz57k-tool` results.

- The published replications keep the core Orz57K tool protocol values:
  `val_size=300`, `eval_size=500`, and `tasks_per_minibatch=3`.
- The run was executed as `orz57k-tool-qwen-ollama`, not the canonical
  production config `orz57k-tool`.
- `gepa_budget.mode` is `light`, so this is an ablation/provenance publish
  rather than a formal medium-budget result for the summary tables.

Published timestamps retained here:

- `20260421T203527Z`
- `20260422T041326Z`
